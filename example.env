# Example Environment Variables for Commander Script
# ------------------------------------------------
# Rename this file to .env in the script's directory (or your project root)
# and fill in your actual values below.
#

# --- Required ---

# Your secret key for the OpenAI API.
# Get one from https://platform.openai.com/api-keys
OPENAI_API_KEY="your_openai_api_key_here"


# --- Optional ---

# Specify the OpenAI model to use.
# If commented out or omitted, the script defaults to "gpt-4o-mini".
# Other examples: "gpt-4o", "gpt-4-turbo", "gpt-3.5-turbo"
# OPENAI_MODEL="gpt-4o-mini"

# Specify the command-line text editor used for the 'edit' ('e') option
# during command execution confirmation.
# If commented out or omitted, the script defaults to 'nano'.
# Examples: "vim", "nvim", "emacs", "code --wait" (for VS Code, requires it in PATH)
# EDITOR="nano"
